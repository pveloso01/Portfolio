# Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Admin User (for initial migration)
ADMIN_EMAIL=admin@portfolio.com
ADMIN_PASSWORD=changeme123

# Security - IP Whitelist
IP_WHITELIST_ENABLED=False
IP_WHITELIST_ADDRESSES=127.0.0.1,::1

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

# Email Configuration
# For development (prints to console):
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# For production (sends real emails via SMTP):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP Settings (Outlook/Hotmail)
EMAIL_HOST=smtp-mail.outlook.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=pedrovelosofernandes@outlook.com
EMAIL_HOST_PASSWORD=your-outlook-app-password-here

# Email Addresses
DEFAULT_FROM_EMAIL=pedrovelosofernandes@outlook.com
CONTACT_EMAIL=pedrovelosofernandes@outlook.com

# Frontend
FRONTEND_URL=http://localhost:3000
